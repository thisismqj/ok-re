apiVersion: apps/v1
kind: Deployment
metadata:
  name: ide
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ide
  template:
    metadata:
      labels:
        app: ide
    spec:
      containers:
      - image: dickhub/linuxserver-codeserver:latest_backdateis_2021-Aug-17_06h30m01s
        name: ide

---

apiVersion: v1
kind: Service
metadata:
  name: ide
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ide"
    port: 8443
  selector:
    app: ide
